{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_3_layout.html.twig' %}
{% block javascripts %}
{{ parent() }}
    <script type="text/javascript" src="{{ asset('js/bootstrap-datepicker.js') }}"></script>
    <script type="text/javascript" src="{{ asset('js/bootstrap-datepicker.ru.min.js') }}"></script>
    <script type="text/javascript" src="{{ asset('js/bootstrap.file_input.js') }}"></script>
{% endblock %}

{% block stylesheet %}
{{ parent() }}
    <link href="{{ asset('css/bootstrap-datepicker.css') }}" rel="stylesheet" type="text/css" media="all" />
    <link href="{{ asset('css/bootstrap-datepicker.min.css') }}" rel="stylesheet" type="text/css" media="all" />
    <link href="{{ asset('css/bootstrap.file_input.css') }}" rel="stylesheet" type="text/css" media="all" />
{% endblock %}

{% block body %}

<div class="single contact">
    <div class="account-top heading">
        <h2>Product creation</h2>
    </div>
    <div class="container">
        <div class="single-main">
            <div class="col-md-9 single-main-left">
                <div class="sngl-top">
                    <div class="col-md-6 single-top-left">
                        {{ form_start(form) }}
                        {{ form_row(form.name) }}
                        {{ form_row(form.category) }}
                        {{ form_row(form.description, {
                            'attr': {
                                'rows': 4,
                            }
                        }) }}
                        {{ form_row(form.sku) }}
                    </div>
                    <div class="col-md-6 single-top-left">
                        {{ form_row(form.price) }}
                        {{ form_row(form.dateCreate) }}
                        {{ form_row(form.dateUpdate) }}
                        <script type="text/javascript">
                            $(function () {
                                $('.datepicker').datepicker({
                                    format: "dd.mm.yyyy",
                                    todayBtn: "linked",
                                    language: "en",
                                    autoclose: true,
                                    locale: "en",
                                    todayHighlight: true
                                });
                            });
                        </script>
                        {{ form_row(form.myRelation) }}
                        {{ form_row(form.isActive) }}
                        {{ form_row(form._token) }}
                    </div>
                </div>
                    <div class="input-group image-preview">
                        <input type="text" class="form-control image-preview-filename" disabled="disabled">
                        <span class="input-group-btn">
                        <button type="button" class="btn btn-default image-preview-clear" style="display:none;">
                            <span class="glyphicon glyphicon-remove"></span> Clear
                        </button>
                        <div class="btn btn-default image-preview-input">
                            <span class="glyphicon glyphicon-folder-open"></span>
                            <span class="image-preview-input-title">Browse</span>
                            {{ form_row(form.image, {
                                'label_attr':{
                                    'class': 'sr-only'
                                }
                            }) }}
                        </div>
                        </span>
                    </div>
            </div>
            <div class="col-md-3 single-right">
                <div class="w_sidebar">
                    <section  class="sky-form">
                        <h4>Action</h4>
                        <div class="row1 row2 scroll-pane">
                            <div class="col col-4">
                                <label>
                                    <input class="btn btn-success" type="submit" value="Create" />
                                    {{ form_end(form, {'render_rest': false}) }}
                                </label>
                                <hr>
                                <label>
                                    <a class="btn btn-info" href="{{ path('product_index') }}">Back to the list</a>
                                </label>
                            </div>
                        </div>
                    </section>
                </div>
            </div>
            <div class="clearfix"> </div>
        </div>
    </div>
</div>
{% endblock %}
